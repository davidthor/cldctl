# Production extension: adds Docker build and overrides command
extends: ./cld.yml

builds:
  api:
    context: .

deployments:
  api:
    image: ${{ builds.api.image }}
    command: ["npm", "start"]
