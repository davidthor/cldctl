# Dev variant - runs locally with hot-reloading
# Usage: cldctl up -c cld.dev.yml

extends: cld.yml

deployments:
  worker:
    runtime: node:20
    command: ["npx", "tsx", "watch", "src/worker.ts"]
    replicas: 1
