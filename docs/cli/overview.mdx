---
title: "CLI Overview"
description: "arcctl command reference"
---

# CLI Overview

arcctl provides a comprehensive CLI for building, pushing, and deploying components and datacenters.

## Installation

```bash
# Install arcctl
go install github.com/architect-io/arcctl@latest

# Verify installation
arcctl --version
```

## Command Structure

arcctl uses an intuitive action-first command structure:

```
arcctl <action> <resource> [arguments] [flags]
```

### Global Flags

| Flag | Description |
|------|-------------|
| `--backend <type>` | State backend type (`local`, `s3`, `gcs`, `azurerm`) |
| `--backend-config <key=value>` | Backend-specific configuration (repeatable) |
| `--help, -h` | Show help for command |
| `--version` | Show version information |

## Commands

### Quick Start

| Command | Description |
|---------|-------------|
| [`arcctl up`](/cli/up) | Quick start for local development |

### Build Commands

| Command | Description |
|---------|-------------|
| [`arcctl build component`](/cli/build/component) | Build a component into OCI artifacts |
| [`arcctl build datacenter`](/cli/build/datacenter) | Build a datacenter into OCI artifacts |

### Deploy Commands

| Command | Description |
|---------|-------------|
| [`arcctl deploy component`](/cli/deploy/component) | Deploy a component to an environment |
| [`arcctl deploy datacenter`](/cli/deploy/datacenter) | Deploy/update a datacenter |

### Destroy Commands

| Command | Description |
|---------|-------------|
| [`arcctl destroy component`](/cli/destroy/component) | Destroy a deployed component |
| [`arcctl destroy datacenter`](/cli/destroy/datacenter) | Destroy a datacenter |
| [`arcctl destroy environment`](/cli/destroy/environment) | Destroy an environment |

### List Commands

| Command | Description |
|---------|-------------|
| [`arcctl list component`](/cli/list/component) | List components |
| [`arcctl list datacenter`](/cli/list/datacenter) | List deployed datacenters |
| [`arcctl list environment`](/cli/list/environment) | List environments |

### Get Commands

| Command | Description |
|---------|-------------|
| [`arcctl get component`](/cli/get/component) | Get component details |
| [`arcctl get datacenter`](/cli/get/datacenter) | Get datacenter details |
| [`arcctl get environment`](/cli/get/environment) | Get environment details |

### Create Commands

| Command | Description |
|---------|-------------|
| [`arcctl create environment`](/cli/create/environment) | Create an environment |

### Update Commands

| Command | Description |
|---------|-------------|
| [`arcctl update environment`](/cli/update/environment) | Update environment (supports config files) |

### Tag Commands

| Command | Description |
|---------|-------------|
| [`arcctl tag component`](/cli/tag/component) | Tag component artifacts |
| [`arcctl tag datacenter`](/cli/tag/datacenter) | Tag datacenter artifacts |

### Push Commands

| Command | Description |
|---------|-------------|
| [`arcctl push component`](/cli/push/component) | Push component artifacts to registry |
| [`arcctl push datacenter`](/cli/push/datacenter) | Push datacenter artifacts to registry |

### Pull Commands

| Command | Description |
|---------|-------------|
| [`arcctl pull component`](/cli/pull/component) | Pull component artifacts from registry |

### Validate Commands

| Command | Description |
|---------|-------------|
| [`arcctl validate component`](/cli/validate/component) | Validate component configuration |
| [`arcctl validate datacenter`](/cli/validate/datacenter) | Validate datacenter configuration |
| [`arcctl validate environment`](/cli/validate/environment) | Validate environment configuration |

## Command Aliases

For convenience, arcctl supports shorthand aliases:

| Full Command | Alias |
|--------------|-------|
| `arcctl build component` | `arcctl build comp` |
| `arcctl deploy datacenter` | `arcctl deploy dc` |
| `arcctl list environment` | `arcctl list env` or `arcctl ls env` |

```bash
# These are equivalent:
arcctl deploy component ./my-app -e staging
arcctl deploy comp ./my-app -e staging

# These are equivalent:
arcctl build datacenter . -t ghcr.io/myorg/dc:v1.0.0
arcctl build dc . -t ghcr.io/myorg/dc:v1.0.0

# These are equivalent:
arcctl list environment
arcctl ls env
```

## State Backends

arcctl supports multiple state backends for storing deployment state:

### Local Backend (Default)

```bash
arcctl deploy component ./my-app -e staging
```

State is stored in `~/.arcctl/state/`.

### S3 Backend

```bash
arcctl deploy component ./my-app -e staging \
  --backend s3 \
  --backend-config bucket=my-arcctl-state \
  --backend-config region=us-east-1 \
  --backend-config key=arcctl
```

### GCS Backend

```bash
arcctl deploy component ./my-app -e staging \
  --backend gcs \
  --backend-config bucket=my-arcctl-state \
  --backend-config prefix=arcctl
```

### Azure Backend

```bash
arcctl deploy component ./my-app -e staging \
  --backend azurerm \
  --backend-config storage_account_name=myaccount \
  --backend-config container_name=arcctl-state \
  --backend-config key=arcctl
```

### Environment Variables

Backend configuration can also be provided via environment variables:

```bash
export ARCCTL_BACKEND=s3
export ARCCTL_BACKEND_S3_BUCKET=my-arcctl-state
export ARCCTL_BACKEND_S3_REGION=us-east-1

arcctl deploy component ./my-app -e staging
```

## Output Formats

Many commands support different output formats:

```bash
# Default table format
arcctl list component -e staging

# JSON output (for scripting)
arcctl list component -e staging --output json

# YAML output
arcctl list component -e staging --output yaml
```

## Exit Codes

| Code | Description |
|------|-------------|
| `0` | Success |
| `1` | General error |
| `2` | Invalid arguments |
| `3` | State locked |
| `4` | Validation error |
| `5` | Resource not found |

## Next Steps

<CardGroup cols={2}>
  <Card title="build component" icon="cube" href="/cli/build/component">
    Build components into OCI artifacts
  </Card>
  <Card title="deploy component" icon="rocket" href="/cli/deploy/component">
    Deploy components to environments
  </Card>
  <Card title="build datacenter" icon="server" href="/cli/build/datacenter">
    Build datacenter infrastructure
  </Card>
  <Card title="Up Command" icon="play" href="/cli/up">
    Quick local development
  </Card>
</CardGroup>
