---
title: "list environment"
description: "List environments"
---

# arcctl list environment

List environments for a datacenter.

<Note>
Use `arcctl ls env` or `arcctl list env` as shorthand.
</Note>

## Synopsis

```bash
arcctl list environment [options]
```

## Options

| Option | Description |
|--------|-------------|
| `-d, --datacenter <name>` | Target datacenter (resolved from flag, `ARCCTL_DATACENTER` env var, or CLI config default) |
| `-o, --output <format>` | Output format: `table`, `json`, `yaml` |
| `--backend <type>` | State backend type |
| `--backend-config <key=value>` | Backend configuration |

<Note>
Environments are scoped to a datacenter. The datacenter is resolved from the `-d` flag, the `ARCCTL_DATACENTER` environment variable, or the `default_datacenter` setting in `~/.arcctl/config.yaml`.
</Note>

## Examples

```bash
# List environments for the default datacenter
arcctl list environment

# Using alias
arcctl ls env

# List environments for a specific datacenter
arcctl list environment -d aws-production

# Output as JSON
arcctl list environment -o json
```

## Output

```
$ arcctl list environment -d aws-production

NAME             DATACENTER           COMPONENTS   CREATED
production       aws-production       5            2024-01-15
staging          aws-production       3            2024-01-10
```

## See Also

- [`arcctl get environment`](/cli/get/environment) - Get environment details
- [`arcctl create environment`](/cli/create/environment) - Create an environment
